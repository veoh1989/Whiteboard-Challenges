{
  "name": "whiteboard14",
  "version": "1.0.0",
  "description": "Write a function called dedupe(head) that accepts the head of a linked list as it's argument, and returns a new linked list - Use a stack to identify any consecutive duplication, and remove them from the list.",
  "main": "index.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "test": "jest"
  },
  "author": "Liza",
  "license": "ISC",
  "dependencies": {
    "jest": "^22.1.4"
  }
}
